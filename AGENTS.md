# AGENTS.md — AI 协作规范

本文件为所有 AI（含 Kilo 及其他编码 Agent）在本仓库工作时的强制规范。除本文件外，请同时遵守全局 `~/.config/kilo/AGENTS.md`。

---

## 1. API 端点文档同步（强制 · 最高优先级）

### 1.1 唯一事实来源

`API_REFERENCE.md` 是本项目**所有 HTTP 端点的唯一事实来源（Single Source of Truth）**，英文版单一文档，内部用醒目注释 banner 分为两部分：

- **Part A — Chat Endpoints**（`/api/chat/*`）：自定义 GUI 接入接口。
- **Part B — Management Endpoints**（`/api/*` 中除 `/api/chat` 外的全部、`/mcp`、`/health`）：项目管理、认证、角色画像、审计、审批、设置、LLM 模型管理、MCP 协议。

### 1.2 强制同步规则

> 🔴 **任何 AI 或开发者只要对任意端点进行了新增、删除或修改，就必须在同一次变更中同步更新 `API_REFERENCE.md`。没有例外。**

"端点增删改"包括但不限于：

- 新增或删除一个 HTTP 路由（含聊天端点与管理端点，含 MCP tool）；
- 修改端点的 **路径 / HTTP 方法 / 认证方式 / 传输方式（JSON / SSE）**；
- 修改 **请求体字段、查询参数、路径参数**（新增、删除、改名、改类型、改必填、改默认值）；
- 修改 **响应字段、状态码、错误码、响应模型**；
- 新增、删除或修改 **SSE 事件类型**（Part A 第 4 节）或其 payload schema；
- 新增、删除或修改 **UI Block 类型**（Part A 第 5 节白名单）；
- 修改端点所属源码文件路径（文档中 `Source:` 标注）。

### 1.3 执行清单（改完代码后逐项确认）

1. 定位变更涉及的端点属于 **Part A** 还是 **Part B**（按命名空间判断）。
2. 更新对应章节的「Endpoint Overview」总览表。
3. 更新对应「Detailed Endpoint Description」小节（请求/响应/参数/错误码/示例）。
4. 若涉及请求或响应模型，同步更新「Request/Response Models」表。
5. 若涉及 SSE 事件或 UI Block，同步更新 Part A 第 4 / 第 5 节。
6. 自检：文档描述与 `backend/app/api/*.py`、`backend/app/mcp/server.py` 中的实际路由定义是否完全一致。

### 1.4 禁止行为

- ❌ 禁止只改代码不改文档。
- ❌ 禁止新建其他 API 文档文件来替代 `API_REFERENCE.md`（保持单一文档）。
- ❌ 禁止把 `API_REFERENCE.md` 拆分回多个语言版本或多份文件。
- ❌ 禁止删除或弱化 Part A / Part B 之间的 banner 注释分隔。

---

## 2. 后端分层架构不变量（建图阶段与执行阶段）

### 2.1 顶层边界

本项目后端必须明确分为两个阶段，二者通过版本化的数据契约连接：

- **建图阶段（Build/Discovery）**：从 OpenAPI、源码 AST、路由表、语义证据以及未来的反编译结果中发现路由和能力，生成可持久化、可追踪、可复现的 `RouteMap` 与 `CapabilityGraph`。
- **执行阶段（Execution）**：接收用户意图，基于指定版本的 `RouteMap` 与 `CapabilityGraph` 形成候选计划，经过参数绑定、策略校验和审批后，通过传输适配器产生外部结果、运行时事件、审计记录和 UI Block。

执行阶段只能依赖建图阶段产出的版本化契约，不得直接读取源码、AST 节点、反编译文本或绑定某一个发现器的内部对象。未来增加发现来源时，应增加输入适配器或证据提供者，不得把来源特有逻辑泄漏到执行层。

当前运行时的主图位于 `backend/app/graph`；`backend/app/orchestrator` 仅作为过渡兼容层。新功能不得继续向已失效的旧阶段节点添加业务逻辑；要替换或删除兼容层时，必须同时迁移调用方、持久化处理和测试。

### 2.2 建图阶段约束

- 发现器必须通过稳定的适配器协议接入，并把“语法定位”和“语义解释”分开。AST 适配器可以提供节点范围、源码片段和结构化证据，但不得假定某一种框架是全局唯一实现。
- 反编译只能作为新的建图输入或证据提供者。它不得改变执行器、策略层、审批协议或 API/MCP 入口的核心契约。
- `route_id`、`capability_id`、RouteMap 版本和 CapabilityGraph 版本必须具有明确的稳定性规则。重建同一来源时，标识应保持稳定；真实身份发生变化时，必须提供显式迁移、别名或失效关系，不能静默生成全新身份。
- `operation_id`、函数名、文件路径等发现结果中的可变字段只能作为显示名或证据，不能在没有迁移规则的情况下直接充当长期身份。
- 每一份图产物必须包含来源指纹、版本、证据引用、置信度、生成时间和构建状态。版本必须不可变且全局唯一，不能依赖可能在同一秒内重复的时间戳作为唯一版本。
- RouteMap 和 CapabilityGraph 的保存、状态发布和旧版本切换必须具有明确的事务边界。重新读取数据库时必须恢复完整的参数、响应和策略元数据，不能只恢复摘要字段。
- 大型源码片段、模型原始结果和中间证据必须遵守敏感信息脱敏、访问控制、生命周期和清理策略；不得把一次建图的原始 LLM 输出无限期当作公开数据保存。

### 2.3 执行阶段约束

执行阶段应遵循以下不可绕过的顺序：

```text
用户意图 -> 候选计划 -> 能力/参数绑定 -> 统一策略门面 -> 审批门 -> 传输适配器 -> 结果/事件/审计
```

- LLM 只能产生候选意图、候选计划、候选参数和候选调用；LLM 返回的安全等级、工具选择或自然语言理由不能替代服务端权限、策略和审批判断。
- HTTP、TCP、本地内存函数以及未来新增的传输方式都必须实现统一的执行门面。门面至少统一处理能力与路由身份、参数绑定、权限、安全策略、审批、超时、取消、重试、幂等键、追踪上下文、响应脱敏、执行 artifact、运行时事件和审计。
- 传输适配器只负责协议握手、编码解码和传输错误映射，不得各自复制计划编排、策略判断、审批恢复、数据库落账或 UI 输出逻辑。
- 所有可能产生外部副作用的调用必须携带可追踪且可复用的幂等标识。重试、网络超时、SSE 重连和 LangGraph interrupt 恢复都不能重复执行同一逻辑写入。
- 只读调用也必须经过统一的身份、超时、追踪、脱敏和审计链路；“只读”只影响风险策略，不代表可以绕过执行门面。
- 失败必须区分参数错误、策略拒绝、审批拒绝、传输失败、上游非成功响应、超时、取消和持久化失败，不能用一个通用成功/失败布尔值覆盖这些状态。

### 2.4 运行状态、事件与持久化

- `TaskRun` 是业务运行记录，负责表达任务生命周期、最终状态和可查询结果；LangGraph checkpoint 只负责可恢复的图状态，不能成为重启、多进程部署或审计的唯一事实来源。
- `TaskEvent` 必须采用可排序、可去重的事件标识，并记录关键状态变化、审批决定、外部副作用意图和执行结果。即时 SSE/MCP 推送是传输方式，不等同于事件持久化。
- `TaskRun`、checkpoint、`TaskEvent`、消息、审批和执行 artifact 必须定义一致的写入顺序、重试语义和恢复规则。任何入口恢复任务时，都必须能够根据持久化状态判断“尚未执行、执行中、已完成或结果未知”，而不是依赖进程内变量猜测。
- 进程内 token 缓存、缓存字典、锁和内存 checkpointer 可以优化性能，但不能作为跨进程、重启后正确性的唯一保障；需要共享一致性的状态必须落到持久化或明确的分布式组件。
- API、Chat、MCP 和 SSE 只是入口或传输层，必须复用同一应用服务、策略门面、执行门面和持久化协议，不能各自实现一套图调用后的落账与事件语义。

### 2.5 安全与协议约束

- 所有执行请求都必须经过服务端身份认证、项目边界校验和策略判断；前端、MCP 客户端或 LLM 传入的“已批准”字段只能作为候选输入，不能直接授予权限。
- A2UI/UI Block 必须继续使用服务端白名单 schema。禁止通过任何接口输出或执行任意 HTML、`script`、`javascript:`、`onclick`、`v-html`、`eval` 或模板表达式；新增 Block 类型必须同步更新协议、校验器、渲染器和 `API_REFERENCE.md`。
- 审批协议必须只有一个规范的数据结构，并明确批准、拒绝、过期、撤销、恢复和重复提交的行为。兼容接口不得使用另一套无法映射的字段名或恢复方式。
- 日志、事件、模型调用记录和执行 artifact 必须遵守最小披露原则。访问令牌、密码、Cookie、API key、完整请求体和敏感响应不得因调试日志或模型原始输出而泄露。

### 2.6 演进规则

- 新协议能力优先扩展稳定的端口和适配器边界，不得让入口层、LLM 客户端或某个具体传输实现成为新的核心抽象。
- 新增端点、MCP 工具、事件、UI Block、请求字段或响应字段时，必须同时更新 `API_REFERENCE.md`、模型校验、相关测试和兼容策略。
- 涉及跨阶段的数据结构变更时，必须先定义版本、兼容读取、迁移和失效策略，再修改生产调用链。
- 架构文档应区分“当前实现”“强制不变量”和“未来方向”，不得把尚未完成的 TCP、本地函数调用、反编译或分布式持久化写成已具备的能力。
- 所有新增代码注释和文档字符串必须使用简体中文；测试必须验证真实行为，包括建图产物完整性、策略/审批分支、恢复去重、事件落账和传输适配器结果，而不只是导入或语法通过。
